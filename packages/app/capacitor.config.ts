import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.yachtnoter',
  appName: 'app',
  webDir: 'dist',
  bundledWebRuntime: true,
};

export default config;
